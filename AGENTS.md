# AI 协作指南

本仓库是产品、设计与研发共用的 monorepo。处理任务前，先根据用户提到的产品进入对应目录；除非用户明确要求跨项目修改，否则不要把一个项目的样式、文案、数据或组件改到其他目录。

## 项目与目录映射

| 目录 | 对应项目 | 代码类型 | 说明 |
| --- | --- | --- | --- |
| `主理人/` | 主理人公社会员小程序 | 原生微信小程序 + Taro H5 | `miniprogram/` 是原生小程序设计源码；`h5/` 是忠实转换的浏览器预览工程。H5 默认展示原登录页。 |
| `温泉/` | 温泉酒店 AI 超级应用 | 原生微信小程序 + Taro H5 | 目录根部的 `pages/`、`components/`、`app.*` 等是原生小程序；`h5/` 是包含 8 个页面和本地演示数据的浏览器预览工程。 |
| `h5-expo/` | 第 34 届北京国际美博会观众预登记 | 纯静态 H5 | 小程序 A 的设计演示：展会首页、预登记、电子入场证、同期论坛。无构建、无后端。 |
| `h5-liren/` | 丽人公社 AI 学习社群 | 纯静态 H5 | 小程序 B 的设计演示：消息、社群、AI 军师、报告、会员方案、门店经营台。无构建、无后端。 |

本机源项目若存在，对应关系如下；这里只是来源说明，不代表自动同步：

- `主理人/` 来源于 `/Users/donis/dev/ai-society/apps/member-app`。
- `温泉/` 来源于 `/Users/donis/dev/ai-business/miniprogram`。
- `h5-expo/`、`h5-liren/` 来源于 `/Users/donis/dev/ai-business/` 下的同名目录。

修改 `ai-univ` 内的协作副本不会自动回写上述源项目。用户要求“改正式业务工程”或“同步回源项目”时，必须明确目标目录后再操作。

## 修改原则

- 目标是保留和走查原产品设计，不要擅自改成通用落地页、重新设计视觉语言或合并不同项目。
- 修改原生小程序时进入对应原生源码；修改浏览器预览时进入对应 `h5/` 或纯静态 H5 目录。
- 如果原生小程序设计发生变化，相关 H5 预览需要同步时，应明确核对页面结构、尺寸、图片、图标和交互，不要只做近似截图稿。
- 微信登录、支付、授权、订阅消息等平台能力不能用 H5 验收替代；H5 只负责设计和浏览器交互走查。
- `h5-expo/` 与 `h5-liren/` 会互相相对跳转，并通过同源 `localStorage` 共享 `lrgs:` 前缀的演示状态。修改路径、存储键或部署子路径时必须同时验证两边。
- 不要提交 `node_modules/`、`dist/`、`.convert/`、`.swc/` 等依赖、构建或转换缓存。
- 不要在子目录初始化新的 `.git`；所有项目都由仓库根目录统一版本管理。

## 本地预览与验证

主理人 H5：

```bash
cd 主理人/h5
npm install
npm run dev
```

温泉 H5：

```bash
cd 温泉/h5
npm install
npm run dev
```

修改这两个 Taro H5 后，至少执行一次：

```bash
npm run build
```

展会与丽人两个纯静态 H5 应从仓库根目录一起启动，以保持同源和相对跳转：

```bash
python3 -m http.server 8765
```

浏览器入口：

- `http://localhost:8765/h5-expo/`
- `http://localhost:8765/h5-liren/`
- 地址末尾加 `?script=1` 可进入演示话术模式。

修改纯静态 H5 后，至少检查入口和关键资源返回 200，并对 `js/*.js` 执行 `node --check`。视觉改动应使用约 390×844 的手机视口检查主要页面、底部导航、弹层以及两个项目之间的跳转。

## 提交边界

- 一个提交或 Pull Request 尽量只包含一个项目目录；根目录规范变更可以单独提交。
- 提交前运行 `git diff --check`，确认没有意外加入缓存、密钥或无关文件。
- 用户只要求某个项目时，不顺手修改其他项目；确需共享改动时，先说明影响范围。
