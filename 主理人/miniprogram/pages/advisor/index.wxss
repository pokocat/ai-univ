/* 服务顾问页版式。 */
.ad-hero { padding: 28rpx; overflow: hidden; }
.ad-title { font-size: 45rpx; font-weight: 700; }
.ad-name { font-size: 35rpx; font-weight: 700; color: var(--ink-900); }
.flow-t { font-size: var(--fs-sec); font-weight: 700; color: var(--ink-900); }
.flow-d { margin-top: 7rpx; font-size: var(--fs-micro); color: var(--ink-600); line-height: 1.45; }
.block { display: block; }
